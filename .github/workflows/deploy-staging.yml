name: Deploy to Staging

on:
  push:
    branches: [develop]
  workflow_dispatch:

env:
  NODE_VERSION: '20'
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}

jobs:
  # Quality gates before deployment
  quality-gates:
    name: Pre-Deployment Quality Gates
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}

      - name: Run CI checks
        run: |
          echo "🔍 Running quality gates..."
          echo "This should trigger CI pipeline first"
          echo "Deployment will only proceed if CI passes"

      - name: Verify CI passed
        run: |
          echo "✅ CI pipeline must pass before deployment"
          echo "- All tests passed"
          echo "- Coverage ≥80%"
          echo "- No linting errors"
          echo "- No type errors"
          echo "- Security audit passed"

  build-and-push:
    name: Build and Push Docker Images
    runs-on: ubuntu-latest
    needs: [quality-gates]
    permissions:
      contents: read
      packages: write
    strategy:
      matrix:
        service: [backend, frontend]
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}-${{ matrix.service }}
          tags: |
            type=ref,event=branch
            type=sha,prefix=staging-
            type=raw,value=staging-latest

      - name: Build and push
        uses: docker/build-push-action@v5
        with:
          context: src/${{ matrix.service }}
          file: src/${{ matrix.service }}/Dockerfile
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
          build-args: |
            NODE_ENV=staging

  deploy-staging:
    name: Deploy to Staging Environment
    runs-on: ubuntu-latest
    needs: build-and-push
    environment:
      name: staging
      url: https://staging.smarterp.com
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Deploy notification
        run: |
          echo "🚀 Deploying to staging environment"
          echo "Branch: ${{ github.ref_name }}"
          echo "Commit: ${{ github.sha }}"
          echo "Actor: ${{ github.actor }}"

      # Add your deployment steps here
      # Examples:
      # - SSH to server and pull new images
      # - Use kubectl to update Kubernetes deployment
      # - Use AWS CLI to update ECS service
      # - Use Terraform/Ansible for infrastructure updates

      - name: Deployment placeholder
        run: |
          echo "⚠️  Deployment steps not configured yet"
          echo "Add your deployment commands here:"
          echo "  - SSH to staging server"
          echo "  - Pull Docker images"
          echo "  - Restart services"
          echo "  - Run database migrations"
          echo "  - Health check"

  health-check:
    name: Health Check
    runs-on: ubuntu-latest
    needs: deploy-staging
    steps:
      - name: Wait for deployment
        run: sleep 30

      - name: Check backend health
        run: |
          echo "Checking backend health..."
          curl -f https://api-staging.smarterp.com/api/health || {
            echo "❌ Backend health check failed"
            exit 1
          }
          echo "✅ Backend is healthy"

      - name: Check frontend
        run: |
          echo "Checking frontend..."
          curl -f https://staging.smarterp.com || {
            echo "❌ Frontend health check failed"
            exit 1
          }
          echo "✅ Frontend is accessible"

      - name: Check database connectivity
        run: |
          echo "Checking database connectivity..."
          curl -f https://api-staging.smarterp.com/api/health/db || {
            echo "❌ Database health check failed"
            exit 1
          }
          echo "✅ Database is connected"

      - name: Check Redis connectivity
        run: |
          echo "Checking Redis connectivity..."
          curl -f https://api-staging.smarterp.com/api/health/redis || {
            echo "❌ Redis health check failed"
            exit 1
          }
          echo "✅ Redis is connected"

  notify:
    name: Notify Deployment
    runs-on: ubuntu-latest
    needs: [deploy-staging, health-check]
    if: always()
    steps:
      - name: Deployment status
        run: |
          if [ "${{ needs.deploy-staging.result }}" == "success" ] && [ "${{ needs.health-check.result }}" == "success" ]; then
            echo "✅ Staging deployment successful!"
            echo "URL: https://staging.smarterp.com"
            echo "API: https://api-staging.smarterp.com"
            echo "Health: https://api-staging.smarterp.com/api/health"
          else
            echo "❌ Staging deployment failed!"
            echo "Deploy: ${{ needs.deploy-staging.result }}"
            echo "Health: ${{ needs.health-check.result }}"
            exit 1
          fi

      # Add Slack/Discord/Email notification here
      # - name: Notify Slack
      #   uses: slackapi/slack-github-action@v1
      #   with:
      #     payload: |
      #       {
      #         "text": "Staging deployment ${{ needs.deploy-staging.result }}"
      #       }
      #   env:
      #     SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
